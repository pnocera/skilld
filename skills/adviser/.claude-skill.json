{
  "name": "adviser",
  "description": "Critical analysis and quality assurance advisor using Claude Agent SDK",
  "version": "2.0.0",
  "entry": "./index.ts",
  "parameters": {
    "taskType": {
      "type": "enum",
      "values": [
        "design-review",
        "plan-analysis",
        "code-verification"
      ],
      "required": true,
      "description": "The type of analysis to perform"
    },
    "input": {
      "type": "string",
      "required": true,
      "description": "Path to input file containing context to analyze"
    },
    "output": {
      "type": "string",
      "default": "",
      "description": "Optional explicit output file path (auto-generated if omitted)"
    },
    "outputDir": {
      "type": "string",
      "default": "",
      "description": "Optional override for output directory (default: docs/reviews/)"
    },
    "mode": {
      "type": "enum",
      "values": [
        "workflow",
        "human",
        "aisp"
      ],
      "default": "aisp",
      "description": "Output mode: 'aisp' (default, AI-to-AI), 'human' (markdown), or 'workflow' (JSON)"
    },
    "timeout": {
      "type": "number",
      "default": 1800000,
      "description": "Execution timeout in milliseconds (default: 30 minutes)"
    }
  }
}