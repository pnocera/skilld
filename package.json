{
  "name": "skilld",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.7",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.25.1"
  },
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "powershell -ExecutionPolicy Bypass -File ./build.ps1",
    "advise": "bun run skills/adviser/index.ts",
    "deploy": "bun run deploy-skill.ts"
  }
}